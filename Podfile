platform :ios, '9.0'

target 'Card Tracker using Realm and TableView' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Card Tracker using Realm and TableView

pod 'RealmSwift'

end
